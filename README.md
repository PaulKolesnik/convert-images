# remove background from all images in a folder using rembg
#Create a Python script that removes backgrounds from all PNG and JPG files in a folder using rembg and saves transparent PNG files into another folder.
